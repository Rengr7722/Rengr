A. The following link contains flow3d setup 
https://www.mediafire.com/file/r60z6ce8kmblweb/Flow3d+11.2+Setup.rar/file

B.  The following link contains video showing patching tutorial and how to obatin your license
https://www.mediafire.com/file/cwpn850z1qkpqe8/License+File+Lecture.rar/file

Patching Notes: ( Same as video above)
0. Uninstall all previous version of flow 3d license servers.
1  Install Flow-3D 11.2. At setup select to install License Server and accept default options for server. If it ask to stop existing server just select skip.
2. If you get blue screen during setup install sentinel drivers given in the setup then reboot and restart flow3d setup.
3. Replace original files with crack files from the crack folder.
4. Place patcher.exe in flow3d  install directory for example. "C:\flow3d\11.2",   and run patcher. Click on Patch.
5. Restart Computer
6. If you get license error, make sure the service "FLOW-3D v11.2 Service" is running in task manager->Services.

Note: If at program start it crashes or there is license error warning run as Administrator "SolidSQUADLoaderEnabler.reg", confirm to add info into Windows registry and reboot computer

powershell command for license / patcher::

$sys=Get-CimInstance Win32_ComputerSystemProduct;$data=("$($sys.IdentifyingNumber)$($sys.Name)$($sys.SKUNumber)$($sys.UUID)$($sys.Vendor)$($sys.Version)" -replace '\s','');$hash=[System.BitConverter]::ToString((New-Object System.Security.Cryptography.MD5CryptoServiceProvider).ComputeHash([System.Text.Encoding]::UTF8.GetBytes($data))).Replace("-","");"{0}-{1}-{2}-{3}-{4}" -f $hash.Substring(0,8),$hash.Substring(8,4),$hash.Substring(12,4),$hash.Substring(16,4),$hash.Substring(20,12) | ForEach-Object { "{$_}" }



